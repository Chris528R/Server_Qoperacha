export interface IPaymentService {

    // Usuario, A que fondo., monto
    // sDevolver todo lo de la api :)
    // doPayment();

    //Usuario, a que fondo, monto, cada cuando
    // Devolver todo lo de la api
    // doRecurrentPayment();

}

